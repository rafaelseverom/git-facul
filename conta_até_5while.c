#include <stdio.h>

int main(){
    
    int cont = 0, valor;
    
    while(cont < 5){
        scanf("%d", &valor);
        cont++; //indica que o usuário digitou mais um valor
    }

    return 0;
}
